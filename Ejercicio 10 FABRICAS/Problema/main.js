// // //declara variables
// var codigo, costoProduccion, materiaPrima, manoObra, gastosFabrica, precioVenta;

// codigo = prompt("Por favor, ingrese codigo de producto");
// materiaPrima = prompt("Por favor, el costo de materia prima");

// // indicar que se lea como num
// materiaPrima = parseFloat(materiaPrima);
// codigo = parseInt(codigo);

// // conseguir manoObra

// if (codigo == 3 || codigo == 4) {
// 	manoObra = materiaPrima * 0.75;
// }
// if (codigo == 1 || codigo == 5) {
// 	manoObra = materiaPrima * 0.80;
// }
// if (codigo == 2 || codigo == 6) {
// 	manoObra = materiaPrima * 0.85;
// }

// // gastosFabrica
// if (codigo == 2 || codigo ==5) {
// 	gastosFabrica = materiaPrima * 0.30;
// }
// if (codigo == 3 || codigo ==6) {
// 	gastosFabrica = materiaPrima * 0.35;
// }
// if (codigo == 1 || codigo ==4) {
// 	gastosFabrica = materiaPrima * 0.28;
// }
//  // costoProduccion

//  costoProduccion = materiaPrima + manoObra + gastosFabrica;

//  // precioVenta
//  precioVenta = costoProduccion + (costoProduccion + 0.45);

//  // mostrar

//  alert("El precio de venta es:  S/. "precioVenta);
 
 // // con funcion-------------------------------------------------------------------------

 var codigo, costoProduccion, materiaPrima, manoObra, gastosFabrica, precioVenta;

 	codigo = prompt("Por favor, ingrese codigo de producto");
	materiaPrima = prompt("Por favor, el costo de materia prima");


